Skin assets are served by the backend from `backend/public/assets/skins`.

These frontend folders document the supported catalog categories for Phase 2
without duplicating bitmap assets. Store previews resolve `PurchaseItem`
`previewUrl` and `metadata.frameAssetUrl` values such as `/assets/skins/...`
against the backend API URL.
